# PostgreSQL connection string
DATABASE_URL=postgresql://rajuan:POSTGRES_PASSWORD@db:5432/rajuan

# Secret for signing JWT sessions (generate with: openssl rand -hex 32)
JWT_SECRET=

# Postgres password (used in docker-compose)
POSTGRES_PASSWORD=

# Admin password (used for seeding only)
ADMIN_PASSWORD=
